# share-store

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build share-store` to build the library.

## Running unit tests

Run `nx test share-store` to execute the unit tests via [Jest](https://jestjs.io).
